#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all
export DEB_CFLAGS_MAINT_APPEND = -Werror=format-security

%:
	dh $@

override_dh_auto_install:
	$(MAKE) install \
		DESTDIR=$(CURDIR)/debian/xscreensaver-screensaver-matrixcode \
		PREFIX=/usr
